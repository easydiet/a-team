${pojo.getPackageDeclaration()}

/**
 * A DAO implementation for ${pojo.getDeclarationName()} objects.
 */
public class ${pojo.getDeclarationName()}DAO 
        extends GenericHibernateDAO<${pojo.getDeclarationName()}, Long>
{
	// implementation in parent class
}